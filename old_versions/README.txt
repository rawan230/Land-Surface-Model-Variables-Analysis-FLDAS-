11 byte-identical duplicate raw downloads (Windows auto-renamed "(1)"/"(2)" copies of
FLDAS_NOAH01_C_GL_M.A*.nc files already present in the parent folder), moved here 2026-09-14.
These are raw source data, never git-tracked (*.nc is gitignored) -- this is filesystem
housekeeping only, not a repo change. Safe to delete outright if disk space is needed; kept
here instead per the project's "archive, don't delete" convention.
